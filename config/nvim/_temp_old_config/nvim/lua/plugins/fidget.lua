-- =====================================================
-- FIDGET.NVIM - LSP PROGRESS NOTIFICATIONS
-- =====================================================
-- Fidget: Shows LSP server progress in the bottom-right corner
-- Provides visual feedback when language servers are indexing, analyzing, or processing

return {
  "j-hui/fidget.nvim", -- LSP progress notifications
  opts = {
    -- Using default configuration for unobtrusive progress display
    -- Shows loading spinners and progress for LSP operations
  },
}
