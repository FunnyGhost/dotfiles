---
description: Commit only the current change and open a draft pull request.
---

Use the `$scoped-pr` skill. Treat `$ARGUMENTS` as optional guidance for identifying the intended change or PR scope.
