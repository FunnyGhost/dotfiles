---
description: Run repo-aware typecheck, lint, format checks, and affected tests for the current change.
---

Use the `$verify` skill for the current change. Treat `$ARGUMENTS` as an optional scope such as `all`, a path, or a workspace.
