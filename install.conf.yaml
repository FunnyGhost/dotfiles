- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/SnappCar Projects
    - ~/Sandbox Projects
    - ~/External Clients

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
