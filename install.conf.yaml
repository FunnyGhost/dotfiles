- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/SnappCar Project
    - ~/Sandbox Projects
    - ~/External Clients

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - echo "ELO!"
  - 
    command: ./test.zsh
    stdout: true
    stderr: true
