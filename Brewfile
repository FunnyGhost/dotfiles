# Taps(repositories)
tap "homebrew/bundle"
tap "homebrew/cask"
tap "homebrew/cask-versions"
tap "homebrew/core"

# Brews(Packages)
brew "bat"
brew "exa"
brew "httpie"

# Casks(Applications)
cask "alfred"
cask "firefox-developer-edition"
cask "visual-studio-code"
cask "webstorm"
